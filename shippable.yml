language: node_js
node_js:
  - 13.12.0


before_script:
  - mkdir -p shippable/testresults
  - mkdir -p shippable/codecoverage


build:
  ci:
    # - shippable_retry npm install -g newman
    # - shippable_retry npm install -g newman-reporter-html
    # - pwd
    # # - newman run newmanapitest.postman_collection.json
    # - newman run newmanapitest.postman_collection.json -r html
    # - cp -r $SHIPPABLE_BUILD_DIR/newman/ ./shippable/testresults
   - shippable_retry npm install
   - pwd
  # - newman run newmanapitest.postman_collection.json
   - npm test
   - cp -r $SHIPPABLE_BUILD_DIR/newman/ ./shippable/testresults